# Full local-development environment. Published dependency metadata is defined
# once in pyproject.toml; release/CI jobs apply an exact constraints file.
-e .[all]
pytest>=8.0,<9.0
build>=1.2,<2.0
pip-tools>=7.4,<8.0
